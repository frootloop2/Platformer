window.Block = {
    create: function(x, y, width, height, color) {
        return {
            x: x,
            y: y,
            dx: 0,
            dy: 0,
            width: width,
            height: height,
            color: color
        };
    }
}